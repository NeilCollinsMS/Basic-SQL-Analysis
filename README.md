# Basic-SQL-Regression
An example linear regression model I generated with Google BigQuery from the public data set 'Medicare'. Mostly a proof of concept, tools such as R would better fit this purpose.
